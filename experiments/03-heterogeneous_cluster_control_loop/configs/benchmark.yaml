run:
  scenarios: [A, B, C]
  jobs: 50
  time_scale: 60
  timeout: 300
  settle_seconds: 30

workload:
  performance_ratio: 0.30
  standard_ratio: 0.50
  best_effort_ratio: 0.20
  gpu_ratio: 0.40

policy:
  eco_cap_cpu_pct: 65
  eco_cap_gpu_pct: 65
  eco_node_fraction: 0.50
