apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: joulie-operator
  namespace: default
  labels:
    release: telemetry
spec:
  namespaceSelector:
    matchNames:
      - joulie-system
  selector:
    matchLabels:
      app.kubernetes.io/name: joulie-operator
  endpoints:
    - port: metrics
      path: /metrics
      interval: 30s
